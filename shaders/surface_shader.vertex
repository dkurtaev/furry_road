attribute vec2 a_position;

varying vec2 v_position;
varying vec4 v_color;

void main() {
  v_position = a_position;
  v_color = gl_Color;
  gl_Position = gl_ModelViewProjectionMatrix * vec4(a_position[0], 0.0,
                                                    a_position[1], 1.0);
}
